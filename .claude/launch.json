{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "websocket-server",
      "runtimeExecutable": "cargo",
      "runtimeArgs": ["run", "--release", "--bin", "websocket_server", "--", "--address", "0.0.0.0", "--port", "8000"],
      "port": 8000
    }
  ]
}
